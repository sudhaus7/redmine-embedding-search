redmine:
  url:
  # rest api key if needed
  # key:
openai:
  # the embedding model to use
  embeddingmodel: text-embedding-3-small

  # base URL if using a non openai.com endpoint
  # baseuri: http://localhost:11434/v1

  # key if needed
  # key: ""

  # the produced embedding dimensions
  # dimensions: 1536
redis:
  connect:
    host: 127.0.0.1
    port: 6379
    database: 0
  vectorkeyname: redmine
